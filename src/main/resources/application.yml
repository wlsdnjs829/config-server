server:
  port: 8091

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wlsdnjs829/config-server
          force-pull: true
          search-paths: '{application}'